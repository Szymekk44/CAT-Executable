using System;

void Main()
{
    string local = "hello";
    Console.WriteLine(local);
    SecondVoid();
    Console.WriteLine("Back to Main()!");
}

void SecondVoid()
{
    string local = "banana";
    Console.WriteLine(local);
    Console.WriteLine("hello from SecondVoid()!");
    string local = "banana";
}